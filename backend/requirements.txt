# Dependencias base (instalar primero)
numpy<2.0  # Mantener en 1.x para compatibilidad con OpenCV y DeepFace

# Dependencias para captura de webcam y procesamiento de imágenes
opencv-python>=4.8.1.78

# Dependencias para reconocimiento emocional facial
tf-keras  # Requerido por DeepFace con TensorFlow 2.17+
deepface

# Dependencias para generación MIDI
mido>=1.3.0

# Dependencias para generación musical con Transformer (PyTorch)
torch>=2.0.0  # PyTorch para el modelo Transformer
miditok>=3.0.0  # Tokenización MIDI (REMI encoding) - requiere v3.0+ para symusic backend
transformers>=4.40,<5  # Hugging Face Transformers para modelo Maestro-REMI (https://huggingface.co/NathanFradet/Maestro-REMI-bpe20k)

# Dependencias para construcción de datasets
datasets>=2.0.0  # HuggingFace datasets para fine-tuning
pandas>=2.0.0  # Procesamiento de CSV con labels
tqdm>=4.65.0  # Progress bars para procesamiento batch

# Dependencias para benchmark y análisis
matplotlib>=3.5.0  # Generación de figuras (heatmaps, gráficos de barras)
seaborn>=0.12.0  # Heatmaps avanzados (confusion matrices)
scipy>=1.10.0  # Métricas estadísticas (KL divergence, correlaciones)
scikit-learn>=1.3.0  # Métricas de clasificación (confusion matrix, accuracy, F1)

# Dependencias para API REST
flask>=3.0.0
flask-cors>=4.0.0
gunicorn
python-dotenv>=1.0.0  # Carga variables de entorno desde archivo .env

# huggingface_hub para descargar modelos desde Hugging Face Hub
huggingface_hub