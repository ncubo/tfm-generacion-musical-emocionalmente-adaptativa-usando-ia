# Dependencias base (instalar primero)
numpy<2.0  # Mantener en 1.x para compatibilidad con OpenCV y DeepFace

# Dependencias para captura de webcam y procesamiento de imágenes
opencv-python>=4.8.1.78

# Dependencias para reconocimiento emocional facial
tf-keras  # Requerido por DeepFace con TensorFlow 2.17+
deepface

# Dependencias para generación MIDI
mido>=1.3.0

# Dependencias para generación musical con Transformer (PyTorch)
torch>=2.0.0  # PyTorch para el modelo Transformer
miditok>=3.0.0  # Tokenización MIDI (REMI encoding) - requiere v3.0+ para symusic backend

# Dependencias para descarga de modelos desde Hugging Face
huggingface_hub>=0.20.0  # Cliente para descargar checkpoints de HF

# Dependencias para API REST
flask>=3.0.0
flask-cors>=4.0.0
gunicorn